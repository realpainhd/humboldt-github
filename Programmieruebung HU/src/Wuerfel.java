import java.util.Random;

public class Wuerfel {

	public static void main(String[] args) {
		int N = Integer.parseInt(args[0]);    //Anzahl an "Augen"
		int K = Integer.parseInt(args[1]);    //Anzahl der direkt hintereinander gewürfelten
		int S = Integer.parseInt(args[2]);    //Anzahl der Spiele
		Random rand= new Random();
		int gewuerfelt = rand.nextInt(N) + 1;  //Kein Würfel hat auf einer Seite weniger als 1 auge
		
		
		
		
		
		
		
	}
	
	
}
